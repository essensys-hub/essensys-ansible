---
- name: Deploy portal nginx snippet
  copy:
    dest: /etc/nginx/snippets/essensys-portal.conf
    mode: '0644'
    content: |
      location /portal/ {
          alias /opt/essensys/portal-frontend/dist/;
          try_files $uri $uri/ /portal/index.html;
      }

      location /api/portal/ {
          proxy_pass http://127.0.0.1:{{ portal_backend_port | default(8081) }}/api/portal/;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
      }

      location /api/gateway/ {
          proxy_pass http://127.0.0.1:{{ portal_backend_port | default(8081) }}/api/gateway/;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
      }

- name: Ensure portal snippet in test server block
  replace:
    path: /etc/nginx/sites-available/essensys
    regexp: '(server_name vps-22806d02\.vps\.ovh\.net test\.essensys\.fr;\n)(?!    include /etc/nginx/snippets/essensys-portal\.conf;)'
    replace: '\1    include /etc/nginx/snippets/essensys-portal.conf;\n'

- name: Ensure portal snippet in production server block
  replace:
    path: /etc/nginx/sites-available/essensys
    regexp: '(server_name mon\.essensys\.fr www\.essensys\.fr gateway\.essensys\.fr essensys\.fr;\n)(?!    include /etc/nginx/snippets/essensys-portal\.conf;)'
    replace: '\1    include /etc/nginx/snippets/essensys-portal.conf;\n'

- name: Reload nginx
  service:
    name: nginx
    state: reloaded
