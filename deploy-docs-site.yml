---
# Deploiement cible docs.essensys.fr + rebuild frontend (lien Documentation)
- name: Deploy documentation site and support frontend
  hosts: essensys
  become: yes

  roles:
    - docs_site
    - frontend

- import_playbook: enable-https-prod.yml
